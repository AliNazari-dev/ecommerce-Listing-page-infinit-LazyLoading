import { useState, useRef, useCallback } from "react";
import ProductsList from "./pages/ProductsList";

const App = () => {
  return (
    <div>
      <ProductsList />
    </div>
  );
};

export default App;
